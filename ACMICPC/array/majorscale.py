import sys
# i 와 i+1, i-1의 차이에 따라 결정
numbers = list(map(int, sys.stdin.readline().split()))
# up 이 계속되면 ascending down이 계속되면 descending, 섞이면 mixed
gap = 0
for i in range(len(numbers)):

    while

#while 을 사용 두 수간의 차이값이 꾸준하는 경우로
